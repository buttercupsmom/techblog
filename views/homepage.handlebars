<div>
  <h2>Tech Blog Posts</h2>
  {{#each posts as |post|}}

    <div>
      <h3>
        <a href='/post/{{post.id}}'>{{post.title}}</a>
      </h3>
      <p>
        Post By:
        {{post.user.name}}
        on
        {{format_date post.date_created}}
      </p>
    </div>
  {{/each}}
</div>